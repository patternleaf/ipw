<h2>The Procedure</h2>
<div class="container">
	<div class="row">
		<ul>
			<li>Why use the word <span class="token keyword">function</span>? Why not <span class="token keyword">procedure</span>?</li>
			<li class="slide">
				<div class="col-md-8">
					<p>
						A function technically accepts <dfn>arguments</dfn> and returns a value. It's a "function" in the mathematical sense: take one thing and turn it into something else.
					</p>
				</div>
				<div class="col-md-2 col-md-offset-2">
					<img src="slides/imgs/function-box-diagram.png" style="width:100%;">
				</div>
			</li>
			<li class="slide">A procedure, by contrast, just <em>does something</em> without mapping or transforming one thing into another.</li>
			<li class="slide">But <span class="token keyword">function</span> is the usual word used for both. That's what we'll use from now on.</li>
		</ul>
	</div>
</div>